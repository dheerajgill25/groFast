# Task brief — E08-Th04-S01-T02

> **Self-contained.** Everything you need for this one task is below. You should not need to read other project-docs files for this task (the project overview is read once at session start; this file handles the rest).

## Project bootstrap (mini)

- **Project:** GroFast (grofast)
- **Tool for this task:** inherit
- **Code goes in:** `code/backend/`
- **PR title format:** `[E08-Th04-S01-T02] <verb> <object>`
- **Branch name:** `task/E08-Th04-S01-T02-<slug>`
- **After merge:** `/task-complete E08-Th04-S01-T02 --pr "#NN"`

## This task

| | |
|---|---|
| **ID** | `E08-Th04-S01-T02` |
| **Title** | Launch config, runbooks & go-live checklist |
| **Description** | Production ops config (zone polygons, store-zone map, ETA params, BR-5 COD cap, low-stock thresholds, partner cohort), on-call rotation + per-alert runbooks, deploy/rollback runbook, compliance checklist (DLT, gateway onboarding, FSSAI/GST, pen-test sign-off). |
| **Owner** | human |
| **Estimate** | M |
| **Depends on** | E08-Th04-S01-T01, E08-Th03-S01-T01, E08-Th02-S01-T02, E01-Th02-S01-T04 |
| **Status** | not_started |
| **Dev tool** | inherit |

## Parent story — E08-Th04-S01

**Title:** As the pod owner, I want the launch city seeded and every runbook written, so that go-live is a checklist, not an adventure

**User story:** _(see 04b_task_breakdown.md story block)_

**Acceptance criteria:**

- [ ] Staging carries synthetic Delhi-NCR data: 60 stores, 50k SKUs, 2k riders, realistic zone polygons, deterministic seeds
- [ ] Production zone polygons, store-zone mapping, ETA params, coupons, COD cap and RBAC users configured before go-live
- [ ] Go-live checklist covers DLT templates, gateway merchant onboarding, FSSAI/GST, pen-test clearance, on-call rotation and rollback

**Linked FRs:** FR-24, FR-21, FR-11
**Linked test cases:** TC-FUNC-028, TC-FUNC-025, TC-INT-021, TC-NEG-013
**Linked screens:** —

## Architecture slices (relevant to this story)

_(no specific architecture sections referenced; see 02b_architecture.md for full architecture)_

## Test cases this task must make pass

### TC-FUNC-028 — Zone polygon publish updates serviceability ≤ 60 s
- **Type:** integration
- **Requirement:** FR-24
- **Preconditions:** Ops user; point P currently unserviceable
- **Steps:** 1. Extend polygon over P 2. Publish 3. New session at P
- **Expected:** New sessions at P serviceable and mapped to correct store within 60 s
- **Priority:** P0

### TC-FUNC-025 — Catalog CRUD live within 60 s; bulk CSV upload
- **Type:** integration
- **Requirement:** FR-21
- **Preconditions:** Catalog-role admin
- **Steps:** 1. Edit SKU price + image 2. Upload 200-row CSV 3. Poll storefront
- **Expected:** Storefront (cache + Meili) reflects changes ≤ 60 s; CSV rows applied with success report
- **Test data:** `fixture `catalog/bulk_200.csv``
- **Priority:** P0

### TC-INT-021 — Order-path trace continuity and SLO alert drill
- **Type:** manual-ops
- **Requirement:** NFR-8
- **Preconditions:** OTel + Prometheus + alerting deployed in staging
- **Steps:** 1. Place traced order 2. Follow trace ID gateway→core→outbox→worker 3. Inject synthetic latency to burn NFR-2 SLO
- **Expected:** One stitched trace across sync + async legs (trace ID propagated in outbox envelope); burn-rate alert pages within policy window and links its runbook
- **Priority:** P1

### TC-NEG-013 — [COND-COD §13] COD above cap blocked
- **Type:** integration
- **Requirement:** FR-11, BR-5
- **Preconditions:** Cart value ₹1,800 > cap ₹1,500
- **Steps:** 1. Select payment method
- **Expected:** COD option hidden/disabled with cap reason; prepaid methods offered; cap value read from ops config
- **Priority:** P1


## Design references

_(no screens linked to this task)_

## Working protocol (inherit-specific)

### Working protocol — generic AI dev tool

Fallback protocol when no tool-specific template exists. Use this as a starting point and override per-tool conventions in your team's playbook.

1. **Read the inputs:**
   - This task brief end-to-end
   - `code/CLAUDE.md` once per session
   - `_briefs/00_PROJECT_OVERVIEW.md` once per session

2. **Branch:** `task/<task-id>-<slug>` (or your tool's equivalent if it doesn't use git directly).

3. **Implement** in the folder indicated by the brief (`code/backend/` or `code/frontend/<feature>/`).

4. **Test:** tag tests with the linked `TC-XXX` IDs so `/test-run-ingest` can update the lifecycle.

5. **PR:** title format `[<task-id>] <verb> <object>`. Use the PR template at `code/.github/PULL_REQUEST_TEMPLATE.md`.

6. **Frontend tools must write `INTEGRATION.md`** in the feature folder when done.

7. **Backend tools must run `/task-complete <id> --pr "#NN"`** after merge.

8. **When in doubt, ask** — don't silently invent fields, endpoints, or designs. The brief contains everything that's been decided.


## Output expectations

- Code change in `code/backend/`
- New tests tagged with the linked TC IDs (`@pytest.mark.tc("TC-XXX")` or framework equivalent)
- PR body filled per `code/.github/PULL_REQUEST_TEMPLATE.md`
- After merge: run `/task-complete E08-Th04-S01-T02 --pr "#NN"` (auto-updates 04b table, Excel, status)
