# Task brief — E03-Th02-S01-T01

> **Self-contained.** Everything you need for this one task is below. You should not need to read other project-docs files for this task (the project overview is read once at session start; this file handles the rest).

## Project bootstrap (mini)

- **Project:** GroFast (grofast)
- **Tool for this task:** inherit
- **Code goes in:** `code/backend/`
- **PR title format:** `[E03-Th02-S01-T01] <verb> <object>`
- **Branch name:** `task/E03-Th02-S01-T01-<slug>`
- **After merge:** `/task-complete E03-Th02-S01-T01 --pr "#NN"`

## This task

| | |
|---|---|
| **ID** | `E03-Th02-S01-T01` |
| **Title** | Domain event emission for order/stock/payment |
| **Description** | Event schemas + in-transaction emission on every order state change, reservation, capture and refund, with actor/reason mandatory for audit. |
| **Owner** | claude-code |
| **Estimate** | M |
| **Depends on** | E03-Th01-S01-T03, E01-Th04-S01-T01 |
| **Status** | not_started |
| **Dev tool** | inherit |

## Parent story — E03-Th02-S01

**Title:** As a downstream system, I want every order/stock/payment state change as a durable event, so that notifications, picking, analytics and audit are decoupled from checkout

**User story:** As a downstream system, I want every order/stock/payment state change as a durable event, so that notifications, picking, analytics and audit are decoupled from checkout

**Acceptance criteria:**

- [ ] `order.placed` / `order.packed` / `payment.captured` / `stock.reserved` etc. are written in the mutation's own transaction
- [ ] Redelivering an event 3× yields exactly one notification, one audit row, one picker-queue entry
- [ ] A full topic replay rebuilds projections without re-sending SMS/push/refunds

**Linked FRs:** FR-12, FR-28
**Linked test cases:** TC-INT-001, TC-INT-003, TC-INT-004
**Linked screens:** —

## Architecture slices (relevant to this story)

_(no specific architecture sections referenced; see 02b_architecture.md for full architecture)_

## Test cases this task must make pass

### TC-INT-001 — Outbox rows commit atomically with the state change
- **Type:** integration
- **Requirement:** FR-12, NFR-10, Arch §5a
- **Preconditions:** Checkout ready
- **Steps:** 1. Confirm payment (success run) 2. Force txn rollback on a second run (inject line shortfall)
- **Expected:** Success: order + `order.placed`/`payment.captured`/`stock.reserved` outbox rows in one commit. Rollback: zero outbox rows — an event can never exist without its state change
- **Priority:** P0

### TC-INT-003 — Consumers are idempotent under duplicate delivery
- **Type:** integration
- **Requirement:** FR-28, Arch §3a C-9
- **Preconditions:** `order.placed` event in topic
- **Steps:** 1. Redeliver the same event 3× to notification, audit, picker-projection consumers
- **Expected:** Exactly one notification, one audit row, one picker-queue entry; duplicates detected via processed-event key and skipped
- **Priority:** P0

### TC-INT-004 — Full topic replay rebuilds projections without duplicate side effects
- **Type:** integration
- **Requirement:** Arch §2a, NFR-10
- **Preconditions:** Picker/analytics projections dropped
- **Steps:** 1. Reset consumer group to offset 0 2. Replay day of events
- **Expected:** Projections rebuilt byte-consistent with Postgres truth; side-effecting consumers (SMS/push/refund) emit nothing on replay
- **Test data:** `seeded topic fixture`
- **Priority:** P1


## Design references

_(no screens linked to this task)_

## Working protocol (inherit-specific)

### Working protocol — generic AI dev tool

Fallback protocol when no tool-specific template exists. Use this as a starting point and override per-tool conventions in your team's playbook.

1. **Read the inputs:**
   - This task brief end-to-end
   - `code/CLAUDE.md` once per session
   - `_briefs/00_PROJECT_OVERVIEW.md` once per session

2. **Branch:** `task/<task-id>-<slug>` (or your tool's equivalent if it doesn't use git directly).

3. **Implement** in the folder indicated by the brief (`code/backend/` or `code/frontend/<feature>/`).

4. **Test:** tag tests with the linked `TC-XXX` IDs so `/test-run-ingest` can update the lifecycle.

5. **PR:** title format `[<task-id>] <verb> <object>`. Use the PR template at `code/.github/PULL_REQUEST_TEMPLATE.md`.

6. **Frontend tools must write `INTEGRATION.md`** in the feature folder when done.

7. **Backend tools must run `/task-complete <id> --pr "#NN"`** after merge.

8. **When in doubt, ask** — don't silently invent fields, endpoints, or designs. The brief contains everything that's been decided.


## Output expectations

- Code change in `code/backend/`
- New tests tagged with the linked TC IDs (`@pytest.mark.tc("TC-XXX")` or framework equivalent)
- PR body filled per `code/.github/PULL_REQUEST_TEMPLATE.md`
- After merge: run `/task-complete E03-Th02-S01-T01 --pr "#NN"` (auto-updates 04b table, Excel, status)
