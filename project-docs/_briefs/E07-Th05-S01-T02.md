# Task brief — E07-Th05-S01-T02

> **Self-contained.** Everything you need for this one task is below. You should not need to read other project-docs files for this task (the project overview is read once at session start; this file handles the rest).

## Project bootstrap (mini)

- **Project:** GroFast (grofast)
- **Tool for this task:** inherit
- **Code goes in:** `code/backend/`
- **PR title format:** `[E07-Th05-S01-T02] <verb> <object>`
- **Branch name:** `task/E07-Th05-S01-T02-<slug>`
- **After merge:** `/task-complete E07-Th05-S01-T02 --pr "#NN"`

## This task

| | |
|---|---|
| **ID** | `E07-Th05-S01-T02` |
| **Title** | Daily digest at 8 am |
| **Description** | Scheduled job composing the KPI digest and delivering via email/Slack, with failure alerting and a re-send command. |
| **Owner** | claude-code |
| **Estimate** | S |
| **Depends on** | E07-Th05-S01-T01, E04-Th05-S01-T01 |
| **Status** | not_started |
| **Dev tool** | inherit |

## Parent story — E07-Th05-S01

**Title:** As a stakeholder, I want KPI dashboards that reconcile with the ledger and a daily digest, so that decisions are data-driven

**User story:** As a stakeholder, I want KPI dashboards that reconcile with the ledger and a daily digest, so that decisions are data-driven

**Acceptance criteria:**

- [ ] Every reported number reconciles with the Postgres order ledger to the rupee; CSV export matches
- [ ] Empty ranges render zeros, never a 5xx
- [ ] Daily digest delivered at 8 am

**Linked FRs:** FR-27
**Linked test cases:** TC-FUNC-031, TC-NEG-031
**Linked screens:** SC-042

## Architecture slices (relevant to this story)

_(no specific architecture sections referenced; see 02b_architecture.md for full architecture)_

## Test cases this task must make pass

### TC-FUNC-031 — KPI report reconciles with order ledger; daily digest
- **Type:** integration
- **Requirement:** FR-27
- **Preconditions:** Warehouse loaded with 1 day of seeded orders
- **Steps:** 1. Load GMV/orders/AOV/SLA/fill-rate report for the day 2. Export CSV 3. Check 8 am digest
- **Expected:** Every number reconciles with Postgres order ledger to the rupee; CSV matches; digest delivered on schedule
- **Test data:** `seeded day fixture `analytics/day1.sql``
- **Priority:** P1

### TC-NEG-031 — Empty date range yields zeroed report, not error
- **Type:** unit
- **Requirement:** FR-27
- **Preconditions:** No orders in range
- **Steps:** 1. Load report for empty range 2. Export CSV
- **Expected:** Zeros/empty states render; CSV valid with headers; no 5xx
- **Priority:** P2


## Design references

### SC-042
**Spec (from 02a_screens.md):**

— Analytics & reports
- **Platform:** web (admin)
- **Purpose:** KPI dashboards and CSV-exportable reports for data-driven decisions.
- **User role(s):** All stakeholders (read scope per role)
- **Data shown:** Date-range picker; KPIs: GMV, orders, AOV, SLA hit rate, fill rate, OOS-after-order, payment success, rider utilization; zone heatmaps; sales reports; daily-digest config (8 am email/Slack)
- **Primary actions:** Change range/filters, export CSV, configure daily digest
- **States:** loading / default / empty (no data in range) / error / edge: numbers reconcile with order ledger (FR-27 a…
⚠️ **`designs/SC-042/` is empty or missing.**


## Working protocol (inherit-specific)

### Working protocol — generic AI dev tool

Fallback protocol when no tool-specific template exists. Use this as a starting point and override per-tool conventions in your team's playbook.

1. **Read the inputs:**
   - This task brief end-to-end
   - `code/CLAUDE.md` once per session
   - `_briefs/00_PROJECT_OVERVIEW.md` once per session

2. **Branch:** `task/<task-id>-<slug>` (or your tool's equivalent if it doesn't use git directly).

3. **Implement** in the folder indicated by the brief (`code/backend/` or `code/frontend/<feature>/`).

4. **Test:** tag tests with the linked `TC-XXX` IDs so `/test-run-ingest` can update the lifecycle.

5. **PR:** title format `[<task-id>] <verb> <object>`. Use the PR template at `code/.github/PULL_REQUEST_TEMPLATE.md`.

6. **Frontend tools must write `INTEGRATION.md`** in the feature folder when done.

7. **Backend tools must run `/task-complete <id> --pr "#NN"`** after merge.

8. **When in doubt, ask** — don't silently invent fields, endpoints, or designs. The brief contains everything that's been decided.


## Output expectations

- Code change in `code/backend/`
- New tests tagged with the linked TC IDs (`@pytest.mark.tc("TC-XXX")` or framework equivalent)
- PR body filled per `code/.github/PULL_REQUEST_TEMPLATE.md`
- After merge: run `/task-complete E07-Th05-S01-T02 --pr "#NN"` (auto-updates 04b table, Excel, status)
