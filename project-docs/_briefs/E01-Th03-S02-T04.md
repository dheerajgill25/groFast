# Task brief — E01-Th03-S02-T04

> **Self-contained.** Everything you need for this one task is below. You should not need to read other project-docs files for this task (the project overview is read once at session start; this file handles the rest).

## Project bootstrap (mini)

- **Project:** GroFast (grofast)
- **Tool for this task:** inherit
- **Code goes in:** `code/backend/`
- **PR title format:** `[E01-Th03-S02-T04] <verb> <object>`
- **Branch name:** `task/E01-Th03-S02-T04-<slug>`
- **After merge:** `/task-complete E01-Th03-S02-T04 --pr "#NN"`

## This task

| | |
|---|---|
| **ID** | `E01-Th03-S02-T04` |
| **Title** | Denied-attempt audit logging |
| **Description** | Every 403 from the RBAC guard emits an audit event with actor, actor_role, attempted action and entity. |
| **Owner** | claude-code |
| **Estimate** | S |
| **Depends on** | E01-Th03-S02-T02, E01-Th03-S03-T01 |
| **Status** | not_started |
| **Dev tool** | inherit |

## Parent story — E01-Th03-S02

**Title:** As an admin owner, I want role-based access with TOTP, so that staff only see their tools and every denial is on the record

**User story:** As an admin owner, I want role-based access with TOTP, so that staff only see their tools and every denial is on the record

**Acceptance criteria:**

- [ ] Roles `ops`, `catalog`, `finance`, `partner`, `read-only`, `super-admin` enforced server-side, never client-only
- [ ] Partner users row-scoped to their own store; ID enumeration returns 403/404
- [ ] Every denied attempt produces an audit row with actor + denied action

**Linked FRs:** FR-29
**Linked test cases:** TC-FUNC-033, TC-NEG-033, TC-SEC-003, TC-SEC-012
**Linked screens:** SC-028, SC-043

## Architecture slices (relevant to this story)

_(no specific architecture sections referenced; see 02b_architecture.md for full architecture)_

## Test cases this task must make pass

### TC-FUNC-033 — Each role sees only its modules
- **Type:** integration
- **Requirement:** FR-29
- **Preconditions:** Users seeded for ops/catalog/finance/partner/read-only
- **Steps:** 1. Log in as each role 2. Enumerate visible modules + attempt one in-scope write each
- **Expected:** Visibility and writes match RBAC matrix exactly; read-only cannot mutate anything
- **Test data:** `fixture `rbac/roles.json``
- **Priority:** P0

### TC-NEG-033 — Finance role hitting catalog-edit API gets 403 + audit
- **Type:** security
- **Requirement:** FR-29
- **Preconditions:** Finance-role JWT
- **Steps:** 1. PUT catalog endpoint directly (bypass UI)
- **Expected:** 403; attempt audit-logged with actor + denied action (FR-29 AC); no data change
- **Priority:** P0

### TC-SEC-003 — Audience claims isolate rider/customer/admin tokens
- **Type:** security
- **Requirement:** NFR-5, FR-29
- **Preconditions:** Valid rider JWT
- **Steps:** 1. Call customer order API and admin API with rider token 2. Vice versa with customer token
- **Expected:** All cross-audience calls 401/403; enforced at gateway guard, not client; attempts logged
- **Priority:** P0

### TC-SEC-012 — Partner users row-scoped to their own store
- **Type:** security
- **Requirement:** FR-29
- **Preconditions:** Partner users P1 (store S1), P2 (store S2)
- **Steps:** 1. As P1, request S2's orders, stock, settlement via UI and raw API (ID enumeration)
- **Expected:** All S2 access 403/404; no data leakage in list endpoints; attempts audit-logged
- **Priority:** P0


## Design references

### SC-028
**Spec (from 02a_screens.md):**

— Admin login (RBAC)
- **Platform:** web (admin)
- **Purpose:** Authenticate staff and resolve role-scoped module access.
- **User role(s):** Ops manager (Neha), catalog, finance, partner manager, picker (Amit), read-only
- **Data shown:** Email/username + password (or SSO placeholder), role-denied messaging
- **Primary actions:** Login, logout (global)
- **States:** default / loading / error (bad credentials, locked) / edge: role lacks module → access-denied interstitial, attempt audit-logged (FR-29)
- **Navigation in:** Dashboard URL, session expiry
- **Navigation out:** SC-029 (ops), SC-044…
⚠️ **`designs/SC-028/` is empty or missing.**

### SC-043
**Spec (from 02a_screens.md):**

— User & role management
- **Platform:** web (admin)
- **Purpose:** Manage staff accounts and role assignments (ops, catalog, finance, partner, picker, read-only).
- **User role(s):** Admin owner
- **Data shown:** User table (name, email, role(s), status, last login), role-permission matrix (read-only view), access audit log (incl. denied attempts)
- **Primary actions:** Invite/create user, assign/revoke roles, deactivate, view audit log
- **States:** loading / default / empty / error / edge: last admin-owner cannot be demoted
- **Navigation in:** Left rail (admin-owner only)
- **Navigation ou…
⚠️ **`designs/SC-043/` is empty or missing.**


## Working protocol (inherit-specific)

### Working protocol — generic AI dev tool

Fallback protocol when no tool-specific template exists. Use this as a starting point and override per-tool conventions in your team's playbook.

1. **Read the inputs:**
   - This task brief end-to-end
   - `code/CLAUDE.md` once per session
   - `_briefs/00_PROJECT_OVERVIEW.md` once per session

2. **Branch:** `task/<task-id>-<slug>` (or your tool's equivalent if it doesn't use git directly).

3. **Implement** in the folder indicated by the brief (`code/backend/` or `code/frontend/<feature>/`).

4. **Test:** tag tests with the linked `TC-XXX` IDs so `/test-run-ingest` can update the lifecycle.

5. **PR:** title format `[<task-id>] <verb> <object>`. Use the PR template at `code/.github/PULL_REQUEST_TEMPLATE.md`.

6. **Frontend tools must write `INTEGRATION.md`** in the feature folder when done.

7. **Backend tools must run `/task-complete <id> --pr "#NN"`** after merge.

8. **When in doubt, ask** — don't silently invent fields, endpoints, or designs. The brief contains everything that's been decided.


## Output expectations

- Code change in `code/backend/`
- New tests tagged with the linked TC IDs (`@pytest.mark.tc("TC-XXX")` or framework equivalent)
- PR body filled per `code/.github/PULL_REQUEST_TEMPLATE.md`
- After merge: run `/task-complete E01-Th03-S02-T04 --pr "#NN"` (auto-updates 04b table, Excel, status)
