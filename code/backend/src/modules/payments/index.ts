/**
 * Public surface of the `payments` module.
 *
 * Everything another module is allowed to touch is re-exported here. Anything
 * under `./internal/` is private and unreachable from outside — enforced by
 * `import/no-restricted-paths` in eslint.config.mjs, not by good intentions.
 *
 * Filled in by the Payments module's own tasks; see ../README.md for the map.
 */
export {};
